# 运维手册

## 资源规划

- 首次构建需要较长时间与较大磁盘空间
- 建议将 `DATA_DIR` 挂载到持久化卷

## 生产建议

- 对外仅暴露需要的 API 与 Bootstrap 页面
- 配置反向代理与访问控制
- 通过定时任务定期重建或更新 DBLP 数据

## 升级流程

1. 备份 `dblp.sqlite`
2. 升级服务镜像
3. 执行构建并验证 `/api/health`、`/api/stats`
4. 再切换上游前端流量
